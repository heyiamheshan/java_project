# Simple Calculation in Java

This Java program defines a simple function to add two numbers and prints the result.

### Script Explanation:
- `addTwoNumbers(int a, int b)`: Adds two numbers and returns the result.
- The script runs a test case where 3 and 5 are added, and the result is printed.
